{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "run": "npm run-script gulp && npm run-script start-server",
    "gulp": "gulp --gulpfile gulpfile.js regular",
    "start-server": "node server.js"
  }
}